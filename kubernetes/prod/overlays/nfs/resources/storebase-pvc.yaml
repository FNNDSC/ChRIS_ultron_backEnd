apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: storebase
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: ""
  resources:
    requests:
      storage: 20Gi
  selector:
    matchLabels:
      volume: storebase
